export function App() {
  return (
    <main className="workspace-shell">
      <section className="workspace-card" aria-labelledby="workspace-title">
        <p className="eyebrow">Workspace</p>
        <h1 id="workspace-title">Policy data workspace</h1>
        <p>
          The application shell is ready for the feed, source configuration, and
          ingestion features delivered in subsequent work.
        </p>
      </section>
    </main>
  );
}
